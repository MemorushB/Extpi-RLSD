#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/_env.sh"

echo "Evaluation orchestration placeholder."
echo "Use tools/extpi_rlsd/aggregate_results.py once checkpoint eval JSON files are available."
